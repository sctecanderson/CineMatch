// Limites de compatibilidade para o cálculo de match
export const FAIXAS_COMPATIBILIDADE = Object.freeze({
  media: 50,
  alta: 80,
});

// Endpoint público da API TVMaze (página 0 do catálogo)
export const API_TVMAZE = "https://api.tvmaze.com/shows?page=0";

// Tempo de rotação do carrossel principal (6,5 segundos)
export const INTERVALO_BANNER = 6500;