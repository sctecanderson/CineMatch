{
  "name": "cinematch-web",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "CineMatch Web - JavaScript puro, Flexbox e TVMaze",
  "scripts": {
    "start": "live-server --port=8080 --no-browser",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "live-server": "1.2.2"
  }
}